import React, { useEffect } from "react";

const ProfilePage = () => {
    useEffect(() => {
        document.title = "JaviKross | Profile";
    }, []);

    return (
        <div>
            PErfil
        </div>
    );
};

export default ProfilePage;
